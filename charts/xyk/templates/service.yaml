apiVersion: v1
kind: Service
metadata:
  name: {{ include "xyk.fullname" . }}
  labels: {{- include "xyk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "xyk.selectorLabels" . | nindent 4 }}
